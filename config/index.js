// @flow

import { type Config } from '../types/config';

const config: Config = {
  siteName: 'My Site',
  layout: 'centered',
  theme: {
    colors: {
      main: 'white',
      dark: 'white',
      light: 'white',
    },
  },
};

export default config;
